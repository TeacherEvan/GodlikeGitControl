# Plan: In-App Git Terminal (TDD)

Date: 2026-07-11

Tasks are ~2-5 min each: write test -> watch fail -> implement -> watch pass ->
commit. Backend + frontend + tests.

## T1 — Backend helper + tests (red first)
- Add `test_09_git_terminal` to `tests/test_suite.py` calling a not-yet-existing
  `/api/git/terminal` endpoint.
- Run suite: expect failure (endpoint missing).

## T2 — Backend implement
- Add `run_git_terminal()` + `GIT_BLOCKED_ARGS` + output cap in `server.py`.
- Register `POST /api/git/terminal` -> `_api_git_terminal` in `handle_api_post`.
- Run suite: T1 passes.

## T3 — Frontend API + controller
- `api.js`: `gitTerminal(path, command)`.
- `terminal.js`: `TerminalView` (output, input, history, help, clear).
- `index.html`: Terminal action button + hidden terminal card in `status-view`.
- `app.js`: `TerminalView.init()`.

## T4 — Verify manually
- Start server, open a repo, run `git status` / `git log --oneline` / `git diff`,
  confirm output renders and stage/unstage checkboxes still work.

## T5 — Commit + clean up
- Commit backend, frontend, tests, docs.
